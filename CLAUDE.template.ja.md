# CLAUDE.md

<!--
━━━ このテンプレートの使い方（使う前にこのコメントブロックごと削除）━━━

1. [例: ...] のプレースホルダを自分のプロジェクトの内容に置き換える
2. 該当しないセクションは見出しごと削除する（空のセクションを残さない）
3. すべての HTML コメントを削除する
  （コメントも毎セッション読み込まれ、コンテキストを消費するため）
4. 全体を短く保つ。目安は 100 行前後、長くなったら @import や skills に分割
5. 削除の判断基準: 「この行を消すと Claude は間違いを犯すか？」→ No なら消す

■ 書く場所を間違えないこと
- 例外なく強制したいルール（main への直接 push 禁止 等）
  → CLAUDE.md ではなく hooks（CLAUDE.md に強制力はない）
- たまにしか使わないドメイン知識・定型ワークフロー
  → CLAUDE.md ではなく .claude/skills/（オンデマンドで読み込まれる）
- コードを読めば分かること、一般論（「きれいなコードを書く」等）
  → どこにも書かない

■ 運用ルール
- Claude が間違えて修正するたびに、該当セクションへ 1 行追加する
- ルールが守られなくなったら「追記」ではなく「削減」を疑う（長すぎのサイン）
- CLAUDE.md に書いてあることを質問してきたら、表現が曖昧なサイン
- コードと同じように定期的にレビューし、効果を観察して剪定する
-->

## 最重要ルール

<!-- 絶対に守ってほしいことを 3〜5 個だけ。冒頭は最も強く効く位置なので厳選する。
すべて断言形で書く（「なるべく」「できれば」は禁止） -->

- [例: 認証情報・API キーは絶対にコミットしない。値は書かず在り処のみ参照する（1Password の dev vault）]
- [例: `packages/legacy/` 配下は変更禁止。修正が必要な場合は必ずユーザーに確認する]
- [例: DB マイグレーションはファイル生成まで。適用（deploy）は人間が行う]

## プロジェクト概要

<!-- 2〜3 行まで。目的・ユーザー・最優先事項があると Claude の判断が事業目線になる -->

[例: 中小企業向け勤怠管理 SaaS（B2B・マルチテナント）。テナント間のデータ分離が最優先事項]

## コマンド

<!-- Claude が推測できないものだけ。`npm install` のような標準コマンドは書かない。
「なぜ標準の方法を使わないか」を一言添えると誤用を防げる -->

```bash
make dev        # ローカル起動（docker compose 経由。npm run dev 直叩きは環境変数不足で失敗する）
make test-unit  # 単体テストのみ（`npm test` は E2E も走り遅いので使わない）
make db-reset   # ローカル DB 初期化（マイグレーション + シード投入）
```

## コードスタイル

<!-- デフォルトや言語の一般規約と「異なる」ものだけを断言形で -->

- [例: ES modules（import/export）を使う。CommonJS（require）は禁止]
- [例: 日付処理は必ず `src/lib/date.ts` を経由する。dayjs を直接 import しない]
- [例: ユーザー向けエラーメッセージは日本語、ログは英語]

## テスト

- [例: テストランナーは vitest。jest の API は使わない]
- [例: 変更に関連する単体テストのみ実行する。全件実行は CI に任せる]
- [例: DB を使うテストはモックせず testcontainers の実 DB を使う]

## リポジトリ作法

- [例: ブランチ名は `feat/123-short-desc` 形式（種別/issue 番号-概要）]
- [例: コミットメッセージは Conventional Commits 準拠]
- [例: PR 本文には実行したテストコマンドとその出力を貼る]

## アーキテクチャ上の決定

<!-- コードを読んでも「なぜそうなっているか」が分からないものだけ。
理由（特に過去の失敗）を一言添えると Claude が勝手に「改善」しなくなる -->

- [例: 認可チェックは必ずサーバー側で行う。クライアント側のみの制御は禁止（2024 年にインシデントあり）]
- [例: 依存方向は `api → services → repositories` の一方向。逆参照を追加しない]

## 環境の癖・既知の落とし穴

- [例: `DATABASE_URL` と `DIRECT_URL` の両方が必須（PgBouncer 経由と直結の使い分け）]
- [例: Apple Silicon はセットアップ手順が異なる → @docs/setup-apple-silicon.md]

## 参照

<!-- 詳細は本文に書かず @import で逃がす（最大 5 階層まで再帰可）。
個人設定は CLAUDE.local.md（非推奨）ではなくホームディレクトリからの import を使う -->

- プロジェクト全体像: @README.md
- Git ワークフロー詳細: @docs/git-workflow.md
- 個人設定（リポジトリにコミットしない）: @~/.claude/my-project-instructions.md
